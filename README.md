# skeletor_web
An online metric extractor tool and repository for traces. 

Clone the repository. 

Make sure that the settings.json file is in the folder with the AWS credentials. 

Then run it using sudo node index.js 
